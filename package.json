{
  "name": "president-sim",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --format=esm --target=es2022 --outfile=assets/app.js --legal-comments=eof",
    "test": "node --test tests/*.test.js",
    "dev": "npm run build && python -m http.server 4173 --bind 127.0.0.1"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
